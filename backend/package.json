{
  "name": "series-finder-backend",
  "version": "1.0.0",
  "description": "Backend API for TV Series Finder application",
  "main": "dist/src/functions/*.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "test": "echo \"No tests yet\"",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@azure/cosmos": "^4.0.1",
    "@azure/functions": "^4.0.0",
    "@azure/identity": "^4.0.1",
    "@azure/storage-blob": "^12.17.0",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "azure-functions-core-tools": "^4.0.0",
    "@types/node": "^20.0.0",
    "@types/uuid": "^9.0.7",
    "rimraf": "^5.0.5",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
